<!--
    Ant build file for the replication module

    $Id: build.xml 15295 2011-09-02 21:04:31Z dizzzz $
-->
<project basedir="." default="jar" name="replication">

    <property name="src" value="./src"/>
    <property name="classes" value="./classes"/>
    <property name="top.dir" value="../.."/>

    <property name="build.compiler" value="modern"/>

    <property file="${top.dir}/build.properties"/>

    <property file="../local.build.properties"/>
    <property file="../build.properties"/>

    <condition property="include.feature.replication.config">
        <istrue value="${include.feature.replication}"/>
    </condition>

    <path id="classpath.core">
        <fileset dir="${top.dir}/${lib.core}">
            <include name="*.jar"/>
        </fileset>
        <fileset dir="${top.dir}/${lib.optional}">
            <include name="*.jar"/>
        </fileset>
        <fileset dir="${top.dir}/${lib.endorsed}">
            <include name="*.jar"/>
        </fileset>
        <fileset dir="${top.dir}/${lib.user}">
            <include name="*.jar"/>
        </fileset>
        <fileset dir="${top.dir}/tools/ant/lib">
            <include name="*.jar"/>
        </fileset>
        <fileset dir="${top.dir}/extensions/replication/lib">
            <include name="*.jar"/>
        </fileset>
        <pathelement path="${top.dir}/exist.jar"/>
        <pathelement path="${top.dir}/exist-optional.jar"/>
        <pathelement path="${top.dir}/start.jar"/>
        <pathelement path="${java.class.path}"/>
    </path>
    
    <!--  if="include.feature.replication.config"
        existdb-contrib fetch task import used for downloading and extracting jar/class from zip
    -->
    <taskdef name="fetch" classname="nl.ow.dilemma.ant.fetch.FetchTask" classpathref="classpath.core" />

    <target name="prepare"  xmlns:ivy="antlib:org.apache.ivy.ant">

        <!--delete file="lib/tika-app-0.9.jar"/-->
        <mkdir dir="${classes}"/>
        
        <fetch classpathref="classpath.core" dest="${top.dir}/tools/ant/lib/" 
            url="http://repo1.maven.org/maven2/org/apache/ivy/ivy/2.2.0/ivy-2.2.0.jar" 
            classname="org.apache.ivy.Main"/>
        
        <taskdef resource="org/apache/ivy/ant/antlib.xml" uri="antlib:org.apache.ivy.ant" classpathref="classpath.core"/>
        
        <ivy:retrieve/>
    </target> 

    <target name="compile" depends="prepare" if="include.feature.replication.config">
        <echo message="----------------------------------"/>
        <echo message="Compiling replication extensions"/>
        <echo message="----------------------------------"/>

        <echo
            message="Compiling with Java ${ant.java.version} from ${build.compiler.source} source to ${build.compiler.target} target, debug ${build.debug}, optimize ${build.optimize}, deprecation ${build.deprecation}"/>

        <javac includeAntRuntime="false" debug="${build.debug}" deprecation="${build.deprecation}" destdir="${classes}"
            encoding="UTF-8" optimize="${build.optimize}" srcdir="${src}" source="${build.compiler.source}"
            target="${build.compiler.target}">
            <classpath>
                <path refid="classpath.core"/>
            </classpath>
        </javac>
    </target>


    <target name="jar" depends="compile" if="include.feature.replication.config">
        <echo message="Creating exist-replication.jar ..."/>
        <jar basedir="${classes}" jarfile="${top.dir}/${lib.extensions}/exist-replication.jar">
            <manifest>
                <attribute name="Project-Name" value="${project.name}"/>
                <attribute name="Project-Version" value="${project.version}"/>
                <attribute name="Project-Build" value="${DSTAMP}"/>
                <attribute name="SVN-Revision" value="${svn.revision}"/>
            </manifest>
        </jar>
    </target>


    <target name="clean" if="include.feature.replication.config">
        <delete dir="${classes}"/>
        <delete file="${top.dir}/${lib.extensions}/exist-replication.jar" failonerror="false"/>
    </target>
</project>
